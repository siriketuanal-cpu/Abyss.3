# ルナビィ移植チェックリスト

## 準備済み

- [x] `index.html`を軽量タイマーの入口として配置
- [x] 互換入口`text-list.html`を配置
- [x] Dot Abyssの独立コア`abyss-lite-core.mjs`を配置
- [x] スターリープの独立コア`starleap-lite-core.mjs`を配置
- [x] 高密度UIの`text-list.js`と`text-list.css`を配置
- [x] ルナビィ専用v2保存アダプタ`lunaby-v2-store.mjs`を配置
- [x] 手動更新導線`update.html`を配置
- [x] ローカル構文検査を実施
- [x] 隔離試作側で計算・保存互換・UI契約の回帰テストを実施
- [x] 旧保存キー`dotabyss:unified:v1`を読込保険として維持

## この準備ディレクトリで未実行の操作

- [x] Gitの追加（ローカルステージ済み）
- [x] Gitのローカルコミット
- [ ] `origin/main`へのpush
- [ ] GitHub Pagesの公開設定
- [ ] 実際のルナビィ公開URLでの初回保存互換確認

## 反映前の最終確認

| 確認対象 | 期待する状態 |
|---|---|
| Dot Abyss | 3分回復、ランク別上限、12時間放置報酬、二段階タップを維持。 |
| スターリープ討伐依頼 | 上限80、12分回復、全回復予定時刻だけを表示。 |
| 御大樹の恵み | 上限4、6時間回復、`123`／`1:23`の全回復入力を維持。 |
| 保存 | `lunaby:state:v2`を優先して読み書きする。初回だけ`dotabyss:unified:v1`を読み、v1は変更・削除しない。 |
| 更新 | 左上の`LUNABY`から`update.html`を開き、古いService Workerと対象キャッシュを解除して入口へ戻る。 |

## v2本格移行の公開前確認

- [x] v1初回取込、v2優先、v1不変、v2破損時のv1復帰を実験環境で確認
- [x] 現在値手入力、二段階40計算、放置報酬、デイリー、STAR LEAP編集をv2で確認
- [x] 3分回復位相・12時間放置・STAR LEAP計算の回帰テストを確認
- [x] 保存文字数84.2%減、20,000回局所保存でv2がv1の約53.5%の時間であることを確認
- [ ] `origin/main`へv2統合を公開
- [ ] GitHub Pagesで初回v1取込とv2優先を確認

> v2公開後は、ルナビィがv1へ逆書込しない。v2の破損時は残っているv1時点から復帰するため、これは旧データを残す保険であって双方向同期ではない。

## アイコン候補の確認

候補Aは青い渦・月・星による抽象的な深淵シンボルで、世界観は強いが小サイズでは線が細かくなる。候補Bは青い深淵の小さなマスコットと星の瞳で、余白が大きく、ランチャー上でもシルエットを認識しやすい。新アプリとしての親しみやすさと小サイズ識別性を優先し、候補Bを本採用候補とする。

候補Bを`icon-192.png`、`icon-512.png`、`icon-maskable-512.png`へ書き出した。実際の192pxファイルでも、紺の丸い深淵マスコット、青い光、星の瞳、月と星のアクセントが区別でき、PWAアイコンとして問題ないことを確認した。
